{
  "compilerOptions": {
    "target": "es2022",                  /* 어떤 버전의 JS로 컴파일할지 지정 (예: es5, es2015, es2022, esnext) */
    "module": "esnext",                  /* 어떤 모듈 시스템을 사용할지 지정 (예: commonjs, es2015, esnext, bun) */
    "moduleResolution": "bun",           /* 모듈을 해석하는 방식 지정 (Node.js 방식, Bun 방식 등) */
    "esModuleInterop": true,             /* CommonJS 모듈과 ES Modules 간의 상호 운용성 활성화 */
    "forceConsistentCasingInFileNames": true, /* 파일 이름 대소문자 일관성 강제 (파일 시스템 불일치 방지) */
    "strict": true,                      /* 모든 엄격한 타입 검사 옵션 활성화 (강력 권장) */
    "skipLibCheck": true,                /* 라이브러리 .d.ts 파일의 타입 검사 건너뛰기 (성능 향상) */
    "outDir": "./dist",                  /* 컴파일된 JS 파일이 생성될 디렉토리 */
    "declaration": true,                 /* .d.ts 타입 정의 파일 생성 여부 */
    "sourceMap": true,                   /* 소스 맵 파일 생성 여부 (디버깅 용이) */
    "baseUrl": "./src",                  /* 모듈 해석의 기준 경로 설정 / 중요: 이 경로를 기준으로 별칭이 작동합니다.  */
    "paths": {
      "@/*": ["./*"]                     /* 추가: @/ 별칭을 src 폴더에 매핑 */
    }
  },
  "include": [
    "src/**/*"                           /* 컴파일에 포함할 파일 패턴 */
  ],
  "exclude": [
    "node_modules",                      /* 컴파일에서 제외할 파일/폴더 */
    "dist"
  ]
}